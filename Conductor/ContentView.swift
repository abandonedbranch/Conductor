import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Conductor v2")
            .frame(minWidth: 600, minHeight: 400)
            .padding()
    }
}

#Preview {
    ContentView()
}
